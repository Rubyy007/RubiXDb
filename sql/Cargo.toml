[package]
name = "rubixdb-sql"
version = "0.1.0"
edition = "2021"
description = "RubiXDB SQL layer — parser, internal AST, binder (no execution)"
publish = false

[dependencies]
rubixdb = { path = ".." }
sqlparser = "=0.63.0"

[dev-dependencies]
proptest = "=1.11.0"
criterion = "=0.8.2"
